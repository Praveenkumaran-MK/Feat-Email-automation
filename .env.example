# ==================== Environment Variables ====================
# Production Email Automation System Configuration
# Copy this file to .env and fill in your actual values

# ==================== BREVO API CONFIGURATION (REQUIRED) ====================
# Get your API key from: https://app.brevo.com/settings/keys/api
# Verify your sender email at: https://app.brevo.com/senders

BREVO_API_KEY=your_brevo_api_key_here
SENDER_EMAIL=your_verified_sender@example.com

# ==================== GMAIL SMTP FOR ERROR NOTIFICATIONS (OPTIONAL) ====================
# For error notifications and daily summaries sent to admin
# Generate App Password: https://myaccount.google.com/apppasswords
# Note: Requires 2-factor authentication enabled on Gmail account

GMAIL_USER=your_gmail@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password
ADMIN_EMAIL=admin@example.com

# ==================== RATE LIMITING CONFIGURATION (OPTIONAL) ====================
# Delay between emails in milliseconds (default: 2000ms = 2 seconds)
# Adjust based on your needs, but stay within Brevo's limits

RATE_LIMIT_DELAY_MS=2000

# ==================== DAILY QUOTA (OPTIONAL) ====================
# Maximum emails to send per day (default: 200)
# Brevo free tier allows 300/day, set lower for safety margin

MAX_EMAILS_PER_DAY=200

# ==================== GITHUB ACTIONS DEPLOYMENT ====================
# For GitHub Actions, add these as GitHub Secrets:
# 1. Go to: Settings → Secrets and variables → Actions
# 2. Add the following secrets:
#    - BREVO_API_KEY
#    - SENDER_EMAIL
#    - GMAIL_USER (optional, for error notifications)
#    - GMAIL_APP_PASSWORD (optional, for error notifications)
#    - ADMIN_EMAIL (optional, for error notifications)
# 3. Do NOT commit .env file to Git (already in .gitignore)

# ==================== NOTES ====================
# - All REQUIRED fields must be set for the system to work
# - OPTIONAL fields enhance functionality but aren't mandatory
# - Gmail notifications are optional - system works without them
# - Rate limiting prevents hitting Brevo API limits
# - Keep this file secure and never commit to Git

